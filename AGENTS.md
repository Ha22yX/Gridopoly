# Gridopoly 协作执行规则

用户最新指令优先。本文件落实用户要求：持续推进未完成任务，并使用 Git 版本控制。

## 任务与续派

- 主任务对用户目标的端到端结果负责，分任务结束只表示该阶段交付完成。
- 开始或恢复工作先读 Docs/firmware/development-coordination-2026-09-07.md 及各端最新进度报告。使用实际可访问的仓库路径，当前为 Z:/Files/我的项目/Gridopoly。
- 用户询问原因、进度或发出纠正，不等于取消原任务。回答后继续此前仍获授权的工作。
- 分任务交付必须说明：完成的具体范围、证据/提交、设备实际版本、未完成事项、阻塞、下一步和负责方。不能仅回复完成后默默丢下依赖。
- 主任务收到阶段交付后，应检查剩余验收并续派明确可执行的下一步；已有任务正在运行时不重复派工。
- 如果所有分任务都已空闲而目标未完成，主任务必须判断并执行下一步，或明确说明唯一阻塞及用户最小操作。不能以等待消息、编译通过或子任务完成为由无故结束整个目标。
- 不要求三个任务始终同时运行；没有独立有效工作时不制造额外测试、文档或相互确认消息来保持活跃。
- 需要实体放置棋子、接线或目视确认时，说明最小操作并保留待验证状态；同时继续不依赖它的工作。不能伪造物理操作或把超时视为用户确认。

## 完成标准

- 分别记录：源码已修改、构建通过、测试通过、已部署版本、真实端到端验收。任何一个都不能替代其他项。
- 当前移动提示/自动到达功能，必须核对正常玩家固件已部署、真实页面呈现后 Action 17 被服务端接受、目标模块 assignment 正确、目标 cue 下发、正确绑定 UID 上报并由权威推进到达，手动确认仍可用。
- 验证绿色目标灯时，先让目标线圈无 Tag，观察提示再放入；Tag 已预先在位时可能直接自动到达并返回 cue=none，不据此断定灯控失败。无需重新插入 Tag 的行为单独验证。
- 源码 Git 检查点和候选构建不能标作正式发布；性能或实机门槛未通过时保留失败结果与限制。
- 玩家屏 RGB 绑核、帧缓冲或传输配置变更需区分帧率、内容正确性及视觉稳定性；帧率和正常串口日志不能证明无闪烁/错位。用户报告显示异常后，重新打开对应验收，记录正常固件及实际页面的目视结果。
- 整体任务仅在用户要求的验收有证据通过，或用户明确取消/改目标后结束。真实外部阻塞应明确报告，不冒充完成。

## 设备与联调

- 玩家屏 COM7 由玩家屏任务独占；格子 COM6 由格子任务独占。串口窗口、烧录及线上操作由主任务依据当前授权协调。
- 已获授权且条件未变的必要操作直接继续，不重复请求确认。持续开发不等于无限授权后台重刷或修改游戏状态。
- 采集设置有界时间，结束后释放串口；记录 reset、失败和时间来源，不掩盖采集扰动。
- 不让用户设备长期停在 SelfTest 结束画面；需要正常游戏时恢复可用固件或使用已验证可启动的正常候选，并明确它是否包含当前修复。
- 恢复旧固件后必须说明哪些新功能随之未部署，主动安排后续验证，不能把恢复旧版本当作修复完成。
- 不擅自注入掷骰、修改玩家资产、房间、模块分配或 Tag 绑定来制造通过证据。

## Git 与文件归属

- 三个开发任务可直接沟通；各自维护职责内文件，主任务统一审核、暂存、提交共享索引。
- 明确文件范围提交，保留其他未提交改动。不在共享目录擅自切分支、reset、clean、stash。
- 凭据、本地备份、构建产物、工具缓存不提交。历史未验证改动与本轮修复的归属如实说明。

## 定期检查

2026-09-08 用户明确要求删除“Gridopoly 未完成工作续派”，automationId=gridopoly 已通过应用工具删除。当前没有该定时检查；未经用户重新明确要求，不得重建或启用替代定时任务。
主任务仍负责当前已授权工作的交接与验收，删除定时任务不代表功能已完成。
