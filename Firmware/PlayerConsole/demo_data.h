#pragma once

#include "app_types.h"

constexpr uint8_t kPlayerCount = 5;
constexpr uint8_t kAssetCount = 5;

extern const PlayerData kPlayers[kPlayerCount];
extern const AssetData kAssets[kAssetCount];
