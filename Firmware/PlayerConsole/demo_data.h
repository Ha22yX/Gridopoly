#pragma once

#include "app_types.h"

constexpr uint8_t kPlayerCount = 5;
constexpr uint8_t kMaxPlayerCount = 6;
constexpr uint8_t kAssetCount = 8;

extern const PlayerData kPlayers[kMaxPlayerCount];
extern const AssetData kAssets[kAssetCount];
